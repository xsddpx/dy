# 模块 04：抖音发布

## 职责

- 上传 `OUTPUT/RUN_ID.mp4`。
- 设置 `自主声明 -> 内容由AI生成`。
- 填写标题、简介和标签。
- 发布后核对创作者中心内容列表中的真实状态。

## 硬阻断

- 未完成 AI 生成声明，不得发布。
- 登录失效、验证码、账号安全、平台风控、上传失败、发布按钮禁用、声明设置失败时停止并报告。
- 不能只因点击了发布按钮就判定成功；必须核对内容列表状态。
