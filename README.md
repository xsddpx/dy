## 规则

- README 只保留简短 DAG。
- 流程变化后及时更新 DAG。

```mermaid
flowchart TD
  A["启动读取"] --> B["建档"]
  B --> C["核心卖点 + 衣柜 + Prompt"]
  C --> D["人物 + 衣柜 + 环境三图生成"]
  D --> E["质检"]
  E --> F["抖音 + 快手发布"]
  F --> G["记录收尾"]
  H["/xyg：服装参考图 → 衣柜商品图入库"] -.-> C
```
