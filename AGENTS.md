# dy auto 项目启动协议

## 启动读取

- 默认读取本文件和 `DOCS/PROJECT.md`，确认本项目默认执行 `anna auto` 完整日更发布流程。
- 需要阶段细节时，按 `DOCS/PROJECT.md` 的固定流程读取 `DOCS/MODULES/` 下对应模块文档。
- 读取 `/Users/Shared/codex/Obsidian Vault/Askill/🎭AGENT须知.md`。

## 固定边界

- 本项目路径固定为 `/Users/Shared/codex/dy`。
- 只保留 `anna` 单人路线和 `auto` 通道。
- `auto` 默认表示：参考抽帧 -> 提示词 -> Kie Nano Banana Pro 1K 确认图 -> 确认图选择 -> Dreamina 双图视频生成 -> 抖音发布 -> 记录收尾。
- 快速通道是显式备用流程：只有用户明确说“快速通道”“跳过确认图”或“备用流程”时才启用；不得因 Kie 失败、时间紧或已有文件存在而自动切换。
- 快速通道表示：参考抽帧 -> grid prompt -> 直接用 `anna.png` + `reference-grid.jpg` 提交 Dreamina -> 抖音发布 -> 记录收尾；跳过 Kie 确认图和确认图选择。
- 项目的两个关键生成节点是确认图和视频生成：确认图决定人物、构图和画面锚点是否可用，视频生成决定最终成片动作、节奏和发布质量；用户说“关键节点”时，仅指这两个关键生成节点，不得自行扩展到参考选择、提示词、发布前等其他流程节点。快速通道启用时，确认图关键节点被明确跳过并记录原因，视频生成仍是关键节点。
- 只执行上述 `anna auto` 主流程或显式快速通道，不接入其他生成路线、角色或兜底工具。
- 每次会话和每次自动化运行默认视为独立新任务；除非用户明确说继续/恢复并指定任务或产物，不读取或续接旧任务状态。
- 不因 `TEMP/` 或 `OUTPUT/` 中存在候选产物而默认发布或续跑旧任务。

## 文档边界

- `DOCS/PROJECT.md` 是核心事实库，记录项目目标、资产、工具和长期边界。
- 视频生成双图输入和提示词规则集中在 `DOCS/MODULES/MODULE_03_DREAMINA_VIDEO.md`。
- 修改核心事实或提示词规则前必须先说明拟修改内容和原因；用户明确要求修改时，本次授权仅限其明确范围。

## 本地提示

- 默认使用中文，实事求是，不虚构未验证结果。
